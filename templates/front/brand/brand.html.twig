{% extends 'base.html.twig' %}

{% block title %}Marque
{% endblock %}

{% block body %}

<div class="container mt-5">
	<div class="border p-5">
		<h1>{{ brand.name }}</h1>
		<p>{{ brand.description }}</p>
		<p><img src="{{ asset('media/'~brand.media) }}" alt="" width="100px"></p>
	</div>
</div>
{% endblock %}