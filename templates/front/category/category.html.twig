{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}

	<div class="container mt-5">
		<div class="border p-5">
			<h1>{{ category.name }}</h1>
			<p>{{ category.description }}</p>
			<p><img src="{{ asset('media/'~category.media) }}" alt="" width="100px"></p>
		</div>
	</div>
{% endblock %}
