{% extends 'base.html.twig' %}

{% block title %}Livre
{% endblock %}

{% block body %}

	<div class="container">
		<div class="border p-5">
			<h1>{{ product.name }}</h1>
			<p>{{ product.price }}</p>
			<p>{{ product.stock }}</p>
		</div>
	</div>
{% endblock %}
